---
title: Assembly language
domain: systems
wikipedia: "Assembly language"
aliases: ["addressing modes", "machine code", "instruction set", "opcode", "immediate addressing", "indexed addressing", "system call", "subroutine call"]
maps_to: [study-hub/ds-core/computing-systems]
---

Programming at the instruction level: how instructions are encoded as machine code, the
addressing modes that say where an operand lives, conditional branches on status flags,
subroutine calls through the stack, and system calls into the operating system.
